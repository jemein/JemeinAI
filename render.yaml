services:
  - type: web
    name: chinese-qa-app
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python3 app.py"
    branch: main
    autoDeploy: true

